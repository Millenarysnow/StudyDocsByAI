# §14.2 自定义复制 — FFastArraySerializer 与状态聚合

> **本节地图**:模块三第 14 章第 2 节。前面看了大规模 actor 的痛点。这一节看 UE 自带的"小武器" — `FFastArraySerializer`,适合复制"对象数组"(玩家库存、技能列表、效果列表)。
>
> 它**不**是 RTS 50K 单位的解(那需要 Mass),但是 RTS 中型场景(数百单位)的好工具。
>
> **预备知识**:§13。
>
> **读完之后你能做到**:
> - 写一个 FFastArraySerializer 数组
> - 知道它的 dirty 检测机制
> - 选定 RTS 项目何时用它

---

## 14.2.1 浅入:用 TArray<T> 复制的痛

```cpp
UPROPERTY(Replicated)
TArray<FInventoryItem> Items;
```

每帧 server diff 时,**整个 array 重新对比**。Items 长 100,即使只改 1 个,也要遍历 100 比较。带宽上发整 array(或 UE 内部 auto-detect 单元素变化,但仍非最优)。

---

## 14.2.2 FFastArraySerializer

```cpp
USTRUCT()
struct FInventoryItem : public FFastArraySerializerItem
{
    GENERATED_BODY()
    
    UPROPERTY()
    FName ItemName;
    
    UPROPERTY()
    int32 Count;
    
    // FFastArraySerializerItem 已定义这些虚函数,可重写
    void PreReplicatedRemove(const struct FInventoryArray& InArray);
    void PostReplicatedAdd(const struct FInventoryArray& InArray);
    void PostReplicatedChange(const struct FInventoryArray& InArray);
};

USTRUCT()
struct FInventoryArray : public FFastArraySerializer
{
    GENERATED_BODY()
    
    UPROPERTY()
    TArray<FInventoryItem> Items;
    
    bool NetDeltaSerialize(FNetDeltaSerializeInfo& DeltaParms)
    {
        return FFastArraySerializer::FastArrayDeltaSerialize<FInventoryItem, FInventoryArray>(Items, DeltaParms, *this);
    }
};

template<>
struct TStructOpsTypeTraits<FInventoryArray> : TStructOpsTypeTraitsBase2<FInventoryArray>
{
    enum { WithNetDeltaSerializer = true };
};
```

复杂的样板,但**核心机制**:每个 item 有 `ReplicationID`(自动分配,稳定),server 端只发"add / remove / change" 的 item 的差量。

---

## 14.2.3 Dirty 标记

```cpp
void AHero::AddItem(FInventoryItem NewItem)
{
    NewItem.ReplicationID = INDEX_NONE;     // 让框架分配
    Inventory.Items.Add(NewItem);
    Inventory.MarkItemDirty(Inventory.Items.Last());
}

void AHero::RemoveItem(int32 Index)
{
    Inventory.Items.RemoveAt(Index);
    Inventory.MarkArrayDirty();    // 标记整 array 变(items 移除时)
}

void AHero::UpdateItem(int32 Index)
{
    Inventory.Items[Index].Count++;
    Inventory.MarkItemDirty(Inventory.Items[Index]);
}
```

---

## 14.2.4 Client 端 PostReplicatedAdd / Remove / Change

```cpp
void FInventoryItem::PostReplicatedAdd(...)
{
    // Client 收到新 item,UI 添加
}

void FInventoryItem::PreReplicatedRemove(...)
{
    // Client 即将移除,UI 通知动画
}

void FInventoryItem::PostReplicatedChange(...)
{
    // Client item 字段变,UI 更新
}
```

---

## 14.2.5 性能

```
TArray 普通复制:O(N) 遍历比较
FFastArraySerializer:O(dirty count) — 只处理变化的 item
```

100 项的 inventory,1 项变 → 只复制 1 项 + 几字节 dirty 标记。

---

## 14.2.6 RTS 用例

```
✅ 玩家技能列表(几个 - 几十个)
✅ 单位状态效果(buff / debuff,几个 / 单位)
✅ 玩家解锁的科技(几十个)

❌ 50K Mass entity 数组(走 Mass 自定义复制,§21)
```

---

## 14.2.7 实操与下一节预告

### 实操

```cpp
// 创建 inventory + add item
ARTSPlayerState->Inventory.Items.Add(NewItem);
ARTSPlayerState->Inventory.MarkItemDirty(LastItem);

// Client 端 PostReplicatedAdd 自动调用
```

### 下一节预告

**§14.3 RTS 网络模型抉择 — Lockstep vs Authoritative + Snapshot** — 大型 RTS 必须做的决策。

---

**到这里你应该具备的能力**:
- [x] 写 FFastArraySerializer 复制 inventory 等数组
- [x] 用 MarkItemDirty / MarkArrayDirty 标脏
- [x] 在 client 用 PostReplicated* 响应变化
- [x] 知道这适合中型(几十-几百)数组,不适合 50K Mass
